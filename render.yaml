services:
  - type: web
    name: interview-prep-hub
    runtime: static
    buildCommand: ""
    staticPublishPath: .
    pullRequestPreviewsEnabled: true
    envVars:
      - key: NODE_ENV
        value: production
